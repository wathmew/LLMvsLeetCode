def strangePrinter(self, s: str) -> int:
    n = len(s)
    dp = [[-1] * n for _ in range(n)]

    def Util(i, j, s, dp):
        if i > j:
            return 0

        if dp[i][j]!= -1:
            return dp[i][j]

        first_letter = s[i]

        answer = 1 + Util(i + 1, j, s, dp)

        for k in range(i + 1, j + 1):
            if s[k] == first_letter:
                # Splitting from i -> k - 1 (remove the last character) and from k + 1 -> j
                better_answer = Util(i, k - 1, s, dp) + Util(k + 1, j, s, dp)
                answer = min(answer, better_answer)

        dp[i][j] = answer
        return answer

    return Util(0, n - 1, s, dp)
