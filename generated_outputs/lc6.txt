class Solution:
    def mergeKLists(self, lists: List[ListNode]) -> Optional[ListNode]:
        # Create a dummy node to simplify the code
        dummy = ListNode(0)
        current = dummy
        
        # Iterate through each list and append its nodes to the main list
        for node in lists:
            if node:
                current.next = node
                current = current.next
            else:
                break
        
        # Merge the sorted lists
        current = dummy.next
        while current.next:
            min_val = current.val
            min_index = current.index(min_val)
            current.val = min_val
            current = current.next
            for i in range(min_index):
                current = current.next
        
        return dummy.next
