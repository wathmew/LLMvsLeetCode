class Solution:
    def mergeKLists(self, lists: List[ListNode]) -> Optional[ListNode]:
        # Create a dummy node to simplify the code
        dummy = ListNode(0)
        current = dummy
        
        # Iterate through the linked lists
        for i in range(len(lists)):
            # If the list is not empty, append its head to the result
            if lists[i]:
                current.next = lists[i]
                current = current.next
            # If the list is empty, skip it
            else:
                continue
        
        # Initialize the current node to the dummy node
        current = dummy
        # Iterate through the linked lists
        while current.next:
            # Move to the next node
            current = current.next
            # Move to the next node in the result
            current.next = lists[current.next]
            # Move to the next node in the result
            current = current.next
        
        # Return the head of the merged linked list
        return dummy.next
